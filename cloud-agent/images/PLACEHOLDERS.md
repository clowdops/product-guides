# Screenshot placeholders

Screenshots still to capture for the ClowdInfra guide. Everything else in this
folder is already captured; the filenames below are referenced from
`<!-- Screenshot: ... -->` comments and will resolve when uncommented.

Capture at a standard desktop width, light theme, against a **demo organisation**
— no real accounts, no real hostnames, no secrets.

| Filename | Page | What to capture |
| --- | --- | --- |
| `artifacts-library.png` | [artifacts.md](../artifacts.md#the-library) | The project's Artifacts tab with three or four entries, at least one showing the **approved** badge and one drafted by the agent |
| `artifact-panel.png` | [artifacts.md](../artifacts.md#opening-one) | The artifact panel with **several versions in the strip**, a ✓ on the approved one, and the diff view open comparing two of them. The diff is what the page is about — a single-version artifact does not show it |

## Also referenced

`cloud-agent/chat.md` describes the [pack chips](../chat.md#active-packs) on the
chat header. Worth capturing alongside the guardrail chips if the header is
re-shot: two chips, one **org** and one **default**, with versions visible.

Shared surfaces reached from this guide — Approvals, Connections, Contacts,
Packs, Signals — are tracked in [`common/images/`](../../common/images/PLACEHOLDERS.md),
not here.
